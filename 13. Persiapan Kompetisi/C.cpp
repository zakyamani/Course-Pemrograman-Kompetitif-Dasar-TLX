#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll N; cin >> N;
    ll term = N;
    if(term % 2 == 0) term--;
    ll result = ((term + 1) * (term + 1))/4;
    

}